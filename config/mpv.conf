# Project-local baseline. This file does not replace the user's mpv settings.
gpu-api=d3d11
hwdec=d3d11va
keep-open=yes
force-window=yes
osd-level=1

# Start with RTX VSR disabled. Use Ctrl+V to toggle it during playback.
